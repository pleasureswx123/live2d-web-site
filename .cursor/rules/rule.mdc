---
alwaysApply: true
---

**1. 语言要求**
- 使用中文进行所有回复、注释和文档说明

**2. 项目架构分析与技术栈一致性**
- 在实施任何解决方案前，先深入分析当前项目的技术栈：React + Zustand + WebSocket + Radix UI + Tailwind CSS
- 最大化复用项目现有的架构模式、工具库和代码结构
- 避免引入与现有技术栈冲突或重复的新依赖
- 保持代码风格、命名规范和目录结构的一致性
- 优先扩展现有的store、组件和工具函数，而非创建新的

**3. 技术选型决策原则**
- 状态管理：优先使用Zustand store，避免Redux或其他状态管理库
- UI组件：优先使用项目中已有的Radix UI组件和自定义组件
- 样式：使用Tailwind CSS类名，保持与现有样式系统一致
- 通信机制：优先使用Zustand store的回调函数和React的状态传递
- 在选择技术方案前，先检查项目中是否已有类似的实现模式

**4. 架构设计与实现思路**
- 采用符合项目现有设计模式的解决方案
- 通过React的useEffect、useState和Zustand store来驱动状态变化和UI更新
- 避免使用全局事件系统（window.addEventListener/dispatchEvent），减少组件间的隐式耦合
- 保持代码的可预测性、可测试性和类型安全性
- 跨组件通信优先通过共享的Zustand store或React Context实现
- 只在与浏览器API交互或第三方库集成等必要场景下才使用原生事件系统

**5. 问题分析与解决方案质量**
- 深入分析问题的根本原因，而非仅处理表面症状
- 提供基于项目实际架构的最优解决方案
- 考虑解决方案的可维护性、可扩展性和性能影响
- 确保代码符合项目的编码规范和最佳实践

**6. 具体实现要求**
- 优先扩展现有的ASR store（useASRStore）的回调函数机制
- 通过Zustand store的状态变化来触发相关的业务逻辑
- 保持与现有WebSocket事件处理模式的一致性
- 确保新增功能与现有的ASR、TTS、聊天等模块协调工作
- 提供清晰的中文注释说明实现逻辑和设计决策