---
alwaysApply: true
---

# 技术决策与代码质量保障规则

## 核心原则

**验证 > 假设，文档 > 经验，测试 > 直觉**

在解决任何技术问题时，必须遵循以下步骤，避免基于错误假设做出决策。

## 问题分析与决策流程

### 1. 问题边界定义
- **技术栈识别**：这个问题涉及哪些具体的技术栈？
- **官方文档查阅**：这些技术栈的官方文档怎么说？
- **社区实践**：是否有相关的源码、示例或社区讨论？
- **问题范围**：这是架构问题、实现问题还是配置问题？

### 2. 假设验证
- **假设来源**：我的假设是基于什么？经验、直觉还是文档？
- **假设验证**：这个假设是否经过实际验证？
- **最小化测试**：能否创建一个最小化测试用例验证假设？
- **反例思考**：是否存在反例可以推翻我的假设？

### 3. 系统性分析
- **避免直觉判断**：不要基于"直觉"或"经验"做决策
- **查阅官方文档**：优先查阅官方文档和API说明
- **查看源码实现**：理解底层实现原理
- **创建测试验证**：用实际代码验证假设

### 4. 认知偏差防范
- **确认偏误**：不要只寻找支持自己观点的信息
- **锚定效应**：不要被相似技术的行为"锚定"
- **可用性启发法**：不要基于容易想到的例子做判断
- **过度自信**：保持开放和怀疑的态度

### 5. 渐进式验证
- **从简单开始**：从最简单的测试用例开始
- **逐步复杂化**：逐步增加复杂度
- **每步验证**：每一步都要验证结果
- **及时调整**：如果验证失败，及时调整方向

### 6. 文档优先原则
- **官方文档优先**：优先查阅官方文档
- **GitHub Issues**：查看相关问题和讨论
- **社区验证**：寻找社区验证的解决方案
- **版本兼容性**：确认版本兼容性

## 状态管理特别规则

### 技术栈识别
- 这是 React Context、Zustand、Redux 还是其他？
- 每个库的更新机制是什么？
- 官方文档如何描述状态更新？

### 同步性验证
- 这个库的 set/update 是同步还是异步？
- 在同一个函数调用中，状态何时可用？
- 能否立即获取更新后的状态？

### 测试驱动开发
```javascript
// 遇到状态管理问题时，先创建这样的测试
const testStore = create((set, get) => ({
  value: null,
  setValue: (v) => {
    console.log('设置前:', get().value)
    set({ value: v })
    console.log('设置后:', get().value)
    console.log('立即获取:', get().value)
  }
}))
```

### 避免类比错误
- 不要将 React setState 的行为类比到其他状态管理库
- 每个库都有自己的设计哲学和实现机制
- 独立分析每个库的特性

## 代码质量保障

### 1. 架构一致性检查
- **技术栈一致性**：是否与项目整体技术栈保持一致？
- **设计模式一致性**：是否遵循项目现有的设计模式？
- **代码风格一致性**：是否与现有代码风格保持一致？
- **命名规范一致性**：是否遵循项目的命名规范？

### 2. 复杂度控制
- **单一职责**：每个函数/组件是否只做一件事？
- **依赖最小化**：是否引入了不必要的依赖？
- **抽象层次**：抽象层次是否合适？
- **可测试性**：代码是否易于测试？

### 3. 可维护性检查
- **可读性**：代码是否易于理解？
- **可扩展性**：是否易于扩展新功能？
- **可调试性**：是否易于调试和排错？
- **文档完整性**：是否有必要的注释和文档？

### 4. 性能考虑
- **不必要的重渲染**：是否避免了不必要的重渲染？
- **内存泄漏**：是否避免了内存泄漏？
- **异步处理**：异步操作是否正确处理？
- **资源管理**：资源是否正确释放？

## 错误防范清单

### 在做出技术决策前，必须回答：

1. **我是否查阅了官方文档？**
2. **我是否创建了测试用例验证假设？**
3. **我是否考虑了反例？**
4. **我的解决方案是否与项目架构一致？**
5. **我是否考虑了长期维护成本？**
6. **我是否避免了过度工程？**
7. **我是否保持了代码的简洁性？**
8. **我是否考虑了向后兼容性？**

### 在编写代码前，必须检查：

1. **是否遵循项目的技术栈选择？**
2. **是否复用了现有的组件和工具？**
3. **是否避免了重复代码？**
4. **是否添加了必要的错误处理？**
5. **是否考虑了边界情况？**
6. **是否添加了必要的日志和调试信息？**
7. **是否考虑了用户体验？**
8. **是否考虑了安全性？**

## 代码审查标准

### 必须拒绝的代码：
- 基于未验证假设的代码
- 与项目架构不一致的代码
- 过度复杂的解决方案
- 缺乏测试的代码
- 违反项目规范的代码
- 性能有明显问题的代码
- 缺乏错误处理的代码

### 必须要求改进的代码：
- 缺乏文档的代码
- 命名不清晰的代码
- 函数过长的代码
- 嵌套过深的代码
- 重复的代码
- 硬编码的代码

## 持续改进

### 定期回顾
- 每周回顾技术决策
- 每月回顾代码质量
- 每季度回顾架构设计
- 每年回顾技术栈选择

### 学习机制
- 记录错误决策的原因
- 分析成功决策的模式
- 更新技术知识库
- 分享经验和教训

### 团队协作
- 代码审查必须严格
- 技术决策需要讨论
- 定期技术分享
- 建立最佳实践文档

## 紧急情况处理

### 当遇到紧急问题时：
1. **不要急于写代码**
2. **先分析问题的根本原因**
3. **寻找最简单的解决方案**
4. **确保解决方案不会引入新问题**
5. **记录问题和解决方案**
6. **后续进行根本性修复**

### 当发现技术债务时：
1. **立即记录问题**
2. **评估影响范围**
3. **制定修复计划**
4. **优先修复高风险问题**
5. **逐步重构低风险问题**
6. **防止新债务产生**

记住：**好的代码是设计出来的，不是调试出来的。好的架构是思考出来的，不是堆砌出来的。**


请记住：验证 > 假设，文档 > 经验，测试 > 直觉
# 技术决策与代码质量保障规则

## 核心原则

**验证 > 假设，文档 > 经验，测试 > 直觉**

在解决任何技术问题时，必须遵循以下步骤，避免基于错误假设做出决策。

## 问题分析与决策流程

### 1. 问题边界定义
- **技术栈识别**：这个问题涉及哪些具体的技术栈？
- **官方文档查阅**：这些技术栈的官方文档怎么说？
- **社区实践**：是否有相关的源码、示例或社区讨论？
- **问题范围**：这是架构问题、实现问题还是配置问题？

### 2. 假设验证
- **假设来源**：我的假设是基于什么？经验、直觉还是文档？
- **假设验证**：这个假设是否经过实际验证？
- **最小化测试**：能否创建一个最小化测试用例验证假设？
- **反例思考**：是否存在反例可以推翻我的假设？

### 3. 系统性分析
- **避免直觉判断**：不要基于"直觉"或"经验"做决策
- **查阅官方文档**：优先查阅官方文档和API说明
- **查看源码实现**：理解底层实现原理
- **创建测试验证**：用实际代码验证假设

### 4. 认知偏差防范
- **确认偏误**：不要只寻找支持自己观点的信息
- **锚定效应**：不要被相似技术的行为"锚定"
- **可用性启发法**：不要基于容易想到的例子做判断
- **过度自信**：保持开放和怀疑的态度

### 5. 渐进式验证
- **从简单开始**：从最简单的测试用例开始
- **逐步复杂化**：逐步增加复杂度
- **每步验证**：每一步都要验证结果
- **及时调整**：如果验证失败，及时调整方向

### 6. 文档优先原则
- **官方文档优先**：优先查阅官方文档
- **GitHub Issues**：查看相关问题和讨论
- **社区验证**：寻找社区验证的解决方案
- **版本兼容性**：确认版本兼容性

## 状态管理特别规则

### 技术栈识别
- 这是 React Context、Zustand、Redux 还是其他？
- 每个库的更新机制是什么？
- 官方文档如何描述状态更新？

### 同步性验证
- 这个库的 set/update 是同步还是异步？
- 在同一个函数调用中，状态何时可用？
- 能否立即获取更新后的状态？

### 测试驱动开发
```javascript
// 遇到状态管理问题时，先创建这样的测试
const testStore = create((set, get) => ({
  value: null,
  setValue: (v) => {
    console.log('设置前:', get().value)
    set({ value: v })
    console.log('设置后:', get().value)
    console.log('立即获取:', get().value)
  }
}))
```

### 避免类比错误
- 不要将 React setState 的行为类比到其他状态管理库
- 每个库都有自己的设计哲学和实现机制
- 独立分析每个库的特性

## 代码质量保障

### 1. 架构一致性检查
- **技术栈一致性**：是否与项目整体技术栈保持一致？
- **设计模式一致性**：是否遵循项目现有的设计模式？
- **代码风格一致性**：是否与现有代码风格保持一致？
- **命名规范一致性**：是否遵循项目的命名规范？

### 2. 复杂度控制
- **单一职责**：每个函数/组件是否只做一件事？
- **依赖最小化**：是否引入了不必要的依赖？
- **抽象层次**：抽象层次是否合适？
- **可测试性**：代码是否易于测试？

### 3. 可维护性检查
- **可读性**：代码是否易于理解？
- **可扩展性**：是否易于扩展新功能？
- **可调试性**：是否易于调试和排错？
- **文档完整性**：是否有必要的注释和文档？

### 4. 性能考虑
- **不必要的重渲染**：是否避免了不必要的重渲染？
- **内存泄漏**：是否避免了内存泄漏？
- **异步处理**：异步操作是否正确处理？
- **资源管理**：资源是否正确释放？

## 错误防范清单

### 在做出技术决策前，必须回答：

1. **我是否查阅了官方文档？**
2. **我是否创建了测试用例验证假设？**
3. **我是否考虑了反例？**
4. **我的解决方案是否与项目架构一致？**
5. **我是否考虑了长期维护成本？**
6. **我是否避免了过度工程？**
7. **我是否保持了代码的简洁性？**
8. **我是否考虑了向后兼容性？**

### 在编写代码前，必须检查：

1. **是否遵循项目的技术栈选择？**
2. **是否复用了现有的组件和工具？**
3. **是否避免了重复代码？**
4. **是否添加了必要的错误处理？**
5. **是否考虑了边界情况？**
6. **是否添加了必要的日志和调试信息？**
7. **是否考虑了用户体验？**
8. **是否考虑了安全性？**

## 代码审查标准

### 必须拒绝的代码：
- 基于未验证假设的代码
- 与项目架构不一致的代码
- 过度复杂的解决方案
- 缺乏测试的代码
- 违反项目规范的代码
- 性能有明显问题的代码
- 缺乏错误处理的代码

### 必须要求改进的代码：
- 缺乏文档的代码
- 命名不清晰的代码
- 函数过长的代码
- 嵌套过深的代码
- 重复的代码
- 硬编码的代码

## 持续改进

### 定期回顾
- 每周回顾技术决策
- 每月回顾代码质量
- 每季度回顾架构设计
- 每年回顾技术栈选择

### 学习机制
- 记录错误决策的原因
- 分析成功决策的模式
- 更新技术知识库
- 分享经验和教训

### 团队协作
- 代码审查必须严格
- 技术决策需要讨论
- 定期技术分享
- 建立最佳实践文档

## 紧急情况处理

### 当遇到紧急问题时：
1. **不要急于写代码**
2. **先分析问题的根本原因**
3. **寻找最简单的解决方案**
4. **确保解决方案不会引入新问题**
5. **记录问题和解决方案**
6. **后续进行根本性修复**

### 当发现技术债务时：
1. **立即记录问题**
2. **评估影响范围**
3. **制定修复计划**
4. **优先修复高风险问题**
5. **逐步重构低风险问题**
6. **防止新债务产生**

记住：**好的代码是设计出来的，不是调试出来的。好的架构是思考出来的，不是堆砌出来的。**


请记住：验证 > 假设，文档 > 经验，测试 > 直觉
